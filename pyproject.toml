[project]
name = "video-feed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "selenium>=4.31.0",
    "pytest>=8.3.5",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
